"""Synthesizer: recommendation, roadmap, risks."""


def run(context: dict) -> dict:
    return {
        "agent": "synthesizer",
        "recommendation": None,
        "roadmap": [],
        "risks": [],
        "notes": "Replace with LLM: merge planner, research, analyst, critic outputs.",
    }
