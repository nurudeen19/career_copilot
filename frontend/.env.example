# Production: full URL to FastAPI (include protocol, no trailing slash).
# VITE_API_BASE_URL=http://localhost:8000
#
# Dev: leave unset to use Vite proxy at /api → http://127.0.0.1:8000 (see vite.config.ts).
